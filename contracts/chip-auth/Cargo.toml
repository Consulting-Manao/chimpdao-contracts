[package]
name = "chimpdao-chip-auth"
edition.workspace = true
license.workspace = true
publish = false
version = "1.0.0"

[lib]
crate-type = ["rlib"]
doctest = false

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
