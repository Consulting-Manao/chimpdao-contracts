# Contributing

Any contributions are welcomed!

## Update contracts

1. Update `.env` with new contract ID
2. Update `.env.example` with new contract ID
3. Update Xcode build settings (`STELLAR_CONTRACT_ID_TESTNET` or `STELLAR_CONTRACT_ID_MAINNET`)
4. Set the network `STELLAR_NETWORK`: `testnet` or `mainnet`
